import { Heading } from '@chakra-ui/react';
import { PageAnimation } from './PageAnimation';

export const Legendaries = () => {
  return (
    <PageAnimation>
      <Heading>Legendaries Page</Heading>
    </PageAnimation>
  );
};
